<nav class="navbar navbar-expand-lg navbar-dark bg-primary nav-blue">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            <a class="navbar-brand logo" href="{{ path('home_index') }}">Go Pharma</a>
        </li>
    </ul>
    {% if app.user %}
        <a class="btn btn-sm btn-danger right" href="{{ path('app_logout') }}"><i class="fas fa-sm fa-sign-out-alt"></i></a>
    {% else %}
        <a class="btn btn-success right" href="{{ path('app_login') }}">Sign in</a>
    {% endif %}
</nav>